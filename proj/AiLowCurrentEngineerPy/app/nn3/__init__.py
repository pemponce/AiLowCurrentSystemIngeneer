"""
app/nn3 — NN-3: размещение устройств → DesignGraph

Модули:
  dataset_gen.py — генератор синтетических планов с разметкой
  model.py       — архитектура GNN (GraphSAGE)
  train.py       — обучение
  infer.py       — инференс (PlanGraph + PreferencesGraph → DesignGraph)
"""